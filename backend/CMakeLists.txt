# Backend CMake configuration
# TODO: Link Boost.Beast and sqlite3 libraries here.

# Add include directories
include_directories(include)

# Find necessary packages (e.g., Boost, Threads)
# find_package(Boost REQUIRED COMPONENTS system thread)
# find_package(Threads REQUIRED)

# Define backend target
# add_executable(backend_server src/main.cpp ...)
# target_link_libraries(backend_server ...)
