// import { useState, useEffect, useRef } from 'react';

/**
 * Custom React Hook for native WebSocket management.
 * 
 * Demonstrates: CN (Client-side Socket Programming)
 */
export function useWebSocket(url) {
  // const [messages, setMessages] = useState([]);
  // const [logs, setLogs] = useState([]);
  // const ws = useRef(null);

  // TODO: Implement useEffect to initialize a native WebSocket connection to 'url'.
  // TODO: Add event listeners for 'onopen', 'onmessage', 'onerror', and 'onclose'.
  
  // TODO: In 'onmessage', parse the incoming JSON payload. 
  // If it's a CHAT message, append to 'messages'. 
  // If it's a LOCK_EVENT or DEADLOCK_DETECTED, append to 'logs'.

  // const sendMessage = (payload) => {
  //   // TODO: Send payload through ws.current if connection is open.
  // };

  return { /* messages, logs, sendMessage */ };
}
